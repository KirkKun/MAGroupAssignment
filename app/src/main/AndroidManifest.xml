<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Financial Wellness"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity android:name=".DebtTrackerActivity" />
        <activity android:name=".FeedbackActivity" />
        <activity android:name=".ConverterActivity" />
        <activity android:name=".SavingsGoalActivity" />
        <activity android:name=".BillReminderActivity" />
        <activity android:name=".SetBudgetActivity" />
        <activity android:name=".AddExpenseActivity" />
        <activity android:name=".ChatbotActivity" />
        <activity android:name=".MoreActivity" />
        <activity android:name=".ChartsActivity" />
        <activity android:name=".WalletActivity" />
        <activity android:name=".HomeActivity" />

        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>